import { inject } from 'vue';
import { UppyContextSymbol } from './context-provider.js';
export function useUppyContext() {
  const context = inject(UppyContextSymbol);
  if (!context) {
    return {
      uppy: undefined,
      status: 'init',
      progress: 0
    };
  }
  return context;
}